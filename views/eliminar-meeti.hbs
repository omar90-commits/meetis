{{>head}}
	
	<main class="contenedor contenedor-formularios">
		<h1>{{nombrePagina}}</h1>
		<form method="POST" action="/eliminar-meeti/{{meeti._id}}" class="default-form">
			<legend>¿Eliminar Meeti? Un vez eliminado no se puede recuperar</legend>

			<div class="campo enviar">
				<input type="submit" value="Eliminar Meeti" class="btn btn-rosa">
			</div>	
		</form>
	</main>

{{>footer}}